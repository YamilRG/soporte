import 'package:flutter/material.dart';

class Laptop_Screen extends StatelessWidget {
  const Laptop_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Laptop_Screen'),
      ),
    );
  }
}
