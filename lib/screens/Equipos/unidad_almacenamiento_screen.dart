import 'package:flutter/material.dart';

class Units_Storage_Screen extends StatelessWidget {
  const Units_Storage_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Units_Storage_Screen'),
      ),
    );
  }
}
