import 'package:flutter/material.dart';

class Impresora_Screen extends StatelessWidget {
  const Impresora_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Impresora_Screen'),
      ),
    );
  }
}
